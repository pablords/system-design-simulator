// Re-export all types from the shared package
export type { 
  ComponentCategory, ComponentType, NodeStatus, ComponentConfig,
  EdgeMetrics, SimulatorEdgeData, NodeMetrics, MetricSnapshot,
  Bottleneck, SimulatorNodeData, SimulationSpeed, SimulationState,
  SavedScenario, ApiProject, CanvasData, ApiUser, AuthResponse, ApiError
} from '@system-design/shared';
